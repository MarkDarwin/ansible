# Folder Structure Conventions

## Goals
Promote predictable, discoverable project layouts.

## Rules
- Use `/src`, `/docs`, `/scripts`, `/tests` where applicable.
- Keep configuration in `/config`.

## Known Gaps
- Multi-service repo patterns
